/*  CREATING ARRAYS


Arrays are used to store multiple values in a single variable, instead of declaring separate variables for each value.

To declare an array,

# Define the variable type,
# Specify the name of the array followed by square brackets and
# Specify the number of elements it should store.
For eg

 int Cars[4] = {1,2,3,4}

Write a program that does the following

# Create an Array - num - of the first 5 positive integers
# Once the array is defined - output "Done" to the console.
*/
#include <stdio.h>

int main() {
  int num[] = {1,2,3,4, 5 };
  printf("Done");
 
  return 0;
}