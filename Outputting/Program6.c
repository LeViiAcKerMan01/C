/* ARITHMETIC OPERATIONS

We can also perform mathematical operations with 'printf()'.
Try printing the sum of 21 and 40
Note - %d is what is known as a format specifier which is used to display integers.
*/
#include <stdio.h>

int main() {
  printf("%d", 21 + 40 );
  return 0;
}